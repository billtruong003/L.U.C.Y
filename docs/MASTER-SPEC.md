# Lucy — MASTER SPEC (version thống nhất toàn bộ core feature) · 2026-06-13

> NGUỒN SỰ THẬT DUY NHẤT để build. Gộp: VISION-JARVIS · PLAN-ALL · PHASE-L/M · ROADMAP-MULTIAGENT-PERSONA ·
> PHASE-E-OVERVIEW · BEYOND-HERMES-IDEAS. Đã bake 2 chỉnh của Bill (#3 minh bạch agent inline, #4 watcher event-driven).
> Nguyên tắc tối cao: **MỘT Lucy thống nhất (Jarvis)** — mọi feature là CHI THỂ của 1 thực thể, KHÔNG phải tab/session rời.
> Mỗi quyết định build hỏi: "cái này làm Lucy THỐNG NHẤT hơn hay phân mảnh thêm?".

---

## PHẦN I — NORTH-STAR: Lucy = Jarvis (5 trụ, đã chỉnh)
1. **Một bộ não — self liên tục**: mọi tương tác (chat/agent/cron/market/code) bồi vào CÙNG context sống về Bill. Nhớ liền mạch ("hôm qua anh làm X…"), không bắt đầu lại.
2. **Một presence, nhiều mặt**: Telegram/Hub/Discord/voice = cùng 1 Lucy, cùng context, liền mạch giữa thiết bị.
3. **Orchestrator-of-self + MINH BẠCH** *(chỉnh)*: Lucy điều phối sub-agent expert; cuộc nói-chuyện-với-agent HIỆN INLINE trong chat dạng **card kiểu bash-tool** (collapse) — Bill thấy Lucy gửi gì cho agent + agent trả gì. Vẫn 1 luồng chat, không phân mảnh, nhưng KHÔNG giấu cơ chế (Bill xem được khi muốn).
4. **Awareness EVENT-DRIVEN** *(chỉnh)*: KHÔNG poll real-time (tốn token). Lucy **tự viết code watcher/hook** (detector nhỏ: lỗi code, deploy fail, ngưỡng giá…) chạy nhẹ/cron, chỉ **PING Lucy khi có sự kiện** → Lucy mới check. Dream chạy đêm. Chủ động nhưng không ồn.
5. **Inward — hiểu Bill sâu** *(CORE/vision)*: học pattern, đoán nhu cầu, continuity, quan hệ sâu dần. Trợ lý cá nhân thật. vault + dream + Context/USER.md = self sống.

## PHẦN II — UI (Jarvis-feel, chống phân mảnh)
- **Hội thoại TRUNG TÂM**, view khác orbit quanh (HUD). Lucy chủ động bật card/galaxy/market/agent-call NGAY trong dòng chat — không bắt nhảy tab.
- Board/Galaxy/Tasks = "Lucy đang cho anh xem" (panel theo ngữ cảnh), không app rời.
- **Home-of-Lucy = trạng thái SỐNG**: đang làm gì · watch gì · vừa học gì (galaxy mini) · task active · + chat.
- Ẩn độ phức tạp (agent/model/session) — Bill trải nghiệm MỘT Lucy. (Cơ chế xem được qua card #3 khi muốn.)

---

## PHẦN III — CORE FEATURE (các chi thể interlock thành 1 hệ)

### CF-1. Unified Self & Context (nền của trụ #1, #5)
- 1 context sống chia sẻ giữa MỌI surface/phiên. Đa-phiên (J) nhưng CÁC PHIÊN CHIA SẺ self chung (Bill + dự án + đang làm gì), không tách Lucy thành nhiều con.
- Gồm: vault (não duy nhất) + recall FTS5 (E2 bookend ✅) + dream/preferences + Context/USER.md.
- Conversation store harness-owned (J1) — nguồn sự thật hội thoại.

### CF-2. Universal Model+Tool Layer = Hermes parity (L + M)
- **L (context portability)**: provider-agnostic request builder → MỌI model (Claude/lane free) nhận CÙNG persona+history → đổi model không đứt mạch. + compressor (E1) ép dưới trần ctx. + catalog scrape (model nào ở nền tảng nào).
- **M (tool harness)**: MỌI model có tool thống nhất (file/bash/**web_fetch/web_search**) + agentic loop → free model = agent KHÔN (research/task dài). Capability-aware routing.
- → Bỏ băng-dán D7. Free model vừa khôn vừa liền mạch.

### CF-3. Orchestrator-of-self + Transparent Multi-agent (K + #3)
- Lucy nhận 1 câu → tự quyết: trả thẳng / hỏi sub-agent expert (persona+skill+brain riêng) / kích autopilot / check market → DỆT về 1 luồng.
- Persona THẬT đa lĩnh vực (dev/marketing/UX/finance/research…), mỗi persona = systemPrompt + skill + **brain riêng (ĐÃ CÓ `Brain/agents/*.md`)**.
- **Minh bạch (#3)**: mỗi lần consult expert → card inline trong chat (gửi gì / nhận gì), collapse. Tái dùng tool-card UI (Section A).
- **Visual brain từng agent (K3)**: thêm `Brain/agents` vào galaxy + view riêng.

### CF-4. Event-driven Awareness (#4)
- Lucy tự viết detector (script/hook/cron nhẹ) cho thứ đáng theo dõi → chỉ ping khi có event → Lucy check. KHÔNG poll real-time.
- Dream đêm (consolidate + prune ✅) + cron lọc tin xàm (Track 4b) chạy nền.
- Chủ động surface đúng lúc, lọc nhiễu mạnh (không spam).

### CF-5. Jarvis UI (Phase N)
- Hội thoại-trung-tâm + orbit + Home-of-Lucy sống (Phần II). Multi-agent card (#3) + galaxy + market panel theo ngữ cảnh.

---

## PHẦN III-B — BEYOND HERMES (hệ sinh thái VƯỢT Hermes) — 📖 chi tiết BEYOND-HERMES-IDEAS.md
> Parity (L+M) = bằng Hermes. Phần này = HƠN Hermes, xây trên "MỘT Lucy" (không đẻ silo). Làm sau parity + K.

- **BH-A. Trí tuệ CHỦ ĐỘNG (event-driven)**: watcher Lucy tự viết (giá/deploy/repo/lịch Fed) → ping khi có biến · self-initiated research chủ đề Bill quan tâm · goal-pursuit dài hạn (autopilot chia task + báo tiến độ). (= trụ #4)
- **BH-B. Galaxy tri thức SỐNG**: node = điểm THẬT (click ra card/file/issue — dream lớp 2) · time-travel não lớn lên theo thời gian · truy vấn visual "Lucy biết gì về X" (galaxy sáng cụm liên quan). (Hermes không có viz)
- **BH-C. Multi-agent VƯỢT**: panel/debate nhiều persona expert tranh luận → tổng hợp (quyết định quan trọng tin cậy hơn) · agent tự chuyên hóa (brain riêng học sâu, đo agent nào giỏi việc gì).
- **BH-D. TỰ CẢI THIỆN (meta-learning — "hơn Hermes" rõ nhất)** ⭐: routing TỰ HỌC từ outcome thật (thay hardcode) · self-review đọc error-stats chính mình → tự sửa prompt/skill · skill auto-author từ pattern lặp · prompt self-tuning.
- **BH-E. Kinh tế/cost thông minh**: cost-aware routing ("Claude ~$X vs lane free?") · ROI mỗi dự án · auto tối ưu budget (việc rẻ → lane).
- **BH-F. Chuyên sâu THỊ TRƯỜNG (sở trường Bill)** ⭐: watchlist real-time + cảnh báo ngưỡng (giá/RSI/funding) · research-agent backtest · sentiment tin tức → Lucy = quant assistant, không generic.
- **BH-G. ĐÁNG TIN & quan sát**: execution replay + explainability ("vì sao Lucy làm X") · self-healing (stuck → tự vá/decompose) · confidence + nguồn + giờ trên mọi câu thị trường.
- **BH-H. Ecosystem**: multi-platform (Discord/WhatsApp như team member) · voice in/out (khi nâng máy) · image/chart understanding (đọc screenshot biểu đồ, tự vẽ chart) · persona registry UI (K4).

> Ưu tiên "wow sớm + rẻ" (build trên nền sẵn): **BH-D (tự học) → BH-F (watchlist thị trường) → BH-B (galaxy node thật) → BH-A (proactive)**.

---

## PHẦN IV — ĐÃ XONG (nền sẵn, 2026-06-13)
Thinking UI (timeline+tool-card — nền cho #3) · Dream prune · **Agent SDK trọn (Hub/bridge/runner/dream-brain)** · Phase E (E1 auto-compact·E2 bookend·E3 badge cache/ctx) · D6 (status offline)·D7 (auto-route band-aid, sẽ thay bằng L) · queue/tasks/nginx/bridge-fix.

## PHẦN V — THỨ TỰ BUILD (đường tới Jarvis)
1. ✅ **K3** — visual brain từng agent (DONE 2026-06-13: galaxy có chòm "Não agent" builder/eng/reviewer)
2. ✅ **J** — chat đa-phiên (DONE 2026-06-13: store chats/<id>.json + sidebar ☰ list/new/switch/rename/delete + migrate chat.json cũ. *Còn: chia sẻ self chung — sẽ hoàn thiện ở L*)
3. ✅ **L + M** — universal context+tool (CF-2) — HERMES PARITY ĐẠT (hub chat)
   - ✅ **L2**: lane chat mang persona + history (verified: gemini-flash/nemotron nhớ qua lượt).
   - ✅ **M**: tool harness cho lane — `web-tools.ts` (web_search/web_fetch, no-key DDG, SSRF-guard) + `lane-chat.ts` (vòng agentic: web/read/list/bash/write/edit, sandbox ws) + coordinator `/chat-lane-agentic` + hub route tool-capable model (M4 từ routeTable) + stream tool-card (#3 minh bạch). VERIFIED: Nemotron tự web_search→web_fetch lấy giá BTC thật + nguồn. smoke:lane PASS.
   - ✅ **L4** catalog scrape (OpenRouter /models): refreshOpenRouterCatalog() fetch background lúc startup + POST /llm/catalog-refresh endpoint. getDynamicCatalog() trả 21 curated + 24 discovered = 45 total. Verified live 2026-06-14.
   - ✅ **L3** compressor lane (DONE 2026-06-14): token-aware sliding window thay cap cứng 24 msg. Budget 5000 tok (4c≈1tok), min 6 verbatim, older → bullet summary trong system. tsc PASS, pm2 restart CLEAN.
   - ✅ follow-up: bridge run_lane thêm history + tool (DONE 2026-06-14): lưu history per (chat_id, model_key), gọi /chat-lane-agentic (web/file/bash), /new xóa cả lane hist. Code PASS syntax+tsc; active khi bridge next restart (không restart per rule).
4. ✅ **K + #3** — orchestrator + multi-agent minh bạch (CF-3): K1 personas (marketing/finance/researcher) ✅ · K2 consult_expert tool trong lane-chat (sub-agent persona+brain, chặn đệ quy, hiện tool-card #3) ✅ verified · K3 visual brain ✅. *Follow-up: ✅ consult cho claude-path (SDK MCP in-process DONE 2026-06-14) · ✅ **K4 persona registry UI** (DONE 2026-06-14): màn QUẢN EXPERT trên Hub (tab 🧩 Experts) CRUD thay vì sửa JSON tay. `persona-config.ts` (sanitize + FS-confined write/delete file `config/personas/<id>.json`, tool-whitelist, chặn path-escape) + store `removePersona` + coordinator `GET/POST/DELETE /personas` (configDir wired) + hub proxy `/api/personas` (GET/POST/DELETE) + web `Personas.tsx` (list/form: id/name/systemPrompt/kind/model/laneModel/realm/tags/tools/maxTurns/timeoutSec, edit/xoá). VERIFY LIVE: GET 17 persona đủ systemPrompt, POST tạo+lọc BadTool khỏi allowedTools, id `../evil` bị từ chối, DELETE xoá file+store, hub 401 khi chưa auth, bundle có tab Experts. tsc agent+server+web PASS, smoke 20/20 + mgmt 52/52 + e2e-http 6/6, pm2 restart coordinator+hub CLEAN.*
5. ⏳ **N** — Jarvis UI (CF-5) ← TIẾP THEO (cần Bill quyết thiết kế — DỪNG hỏi)
6. ⏳ **#4** — event-driven watchers (Lucy tự viết code) (CF-4)
   - ✅ **#4-infra** — watcher framework (DONE 2026-06-14): watcher.ts (4 kind: health/disk/process/price + cooldown + Telegram alert) + watcher-cli.ts (--dry-run) + config/watchers.json (3 system watchers enabled + 3 price slots disabled). tsc PASS, smoke 20/20, watcher dry-run 3/3 PASS. Cron entry can Bill them: */30 * * * * node_modules/.bin/tsx src/watcher-cli.ts
7. **BEYOND HERMES (Phần III-B)** — vượt Hermes, ưu tiên: BH-D tự học → BH-F watchlist thị trường → BH-B galaxy node thật → BH-A proactive (rồi BH-C/E/G/H)
   - ✅ **BH-D routing tự học** — (DONE 2026-06-14): đóng vòng meta-learning. `routing-outcome.ts` thêm `pickLearnedModel()` (override con đầu bảng hardcode CHỈ khi candidate đủ MIN_SAMPLES=4 + hơn head ≥10% — chống thrash). `chat-lane.routeTask` đọc outcomes → `RouteDecision.learned` + ghi vết lý do (BH-G explainability). Coordinator: `POST /llm/feedback` (👍/👎 → vault `Brain/routing-outcomes.jsonl`), `GET /llm/outcomes` (bảng score), `/route` tự nạp outcomes. Hub proxy `/api/llm/feedback` + `/api/llm/outcomes`. `self-review-cli.ts` (script `self-review`) gộp error-stats + outcomes ra report .md. smoke:routing 15/15, tsc agent+hub PASS. VERIFY LIVE: feed 4👍 nemotron + 4👎 ds → `/route` reasoning override `ds-v4-flash-free`(head)→`or-nemotron-super` learned=true. (Còn: nút 👍/👎 trong Hub UI = N/Jarvis-UI, cần Bill.)
   - ✅ **BH-F RSI watcher** — (DONE 2026-06-14): kind `rsi` trong watcher.ts (CoinGecko market_chart + RSI(14) tinh toan); BTC RSI watcher enabled (nguong <30 qua ban, >70 qua mua, cooldown 12h), ETH disabled. Verify live: BTC RSI(14)=26.00 <= 30 ALERT, smoke 20/20, tsc PASS.
   - ✅ **BH-B galaxy tri thức SỐNG (hoàn thiện)** — (DONE 2026-06-14): 3 mảnh trong `Galaxy.tsx` (data có sẵn `mtime`/node, không đổi backend contract):
     - **Time-travel**: thanh tua dưới đáy → lọc node theo `mtime ≤ asOf` ("não lớn lên theo thời gian"); kéo về quá khứ → tinh hà nhỏ lại, kéo về GIỜ → đầy đủ; ghost (note chưa viết) giữ nếu có node-hiện trỏ tới; node tái-xuất-hiện animate ló sáng. Verify: vault thật 289 node 02→14/06, as-of giữa kỳ = 145/289 node.
     - **Visual query "Lucy biết gì về X"**: recall → cụm khớp (hit + liên quan 1 bước wikilink) SÁNG (uBright×1.3), phần còn lại MỜ (×0.07) + label tắt; nút ✕ bỏ focus, gõ rỗng tự bỏ. (Trước chỉ pulse ngắn.)
     - **Tuổi node trong hover**: "học X ngày/tháng trước" (từ `mtime`) bổ trợ narrative thời gian.
     - tsc web PASS, build PASS, lucy-hub restart CLEAN serve bundle mới. (node→card/issue: hiện click→file vault; card/issue link = khi có brain agent node trỏ ra ngoài, để sau.)
   - ✅ **BH-G ĐÁNG TIN & quan sát (replay + self-healing)** — (DONE 2026-06-14, verify+deploy round): 2 chi thể, code đã có từ trước nay VERIFY + BẬT LIVE:
     - **Execution replay (turn-log)**: `turn-log.ts` (`createTurnLogger` opt-in env `AM_TURNS_LOG`=thư mục → `<dir>/turn-log.jsonl`, no-op nếu k set) + wire `lane-runner.ts` (per-turn: motive/action/outcome/error/token/model THỰC) + `runner.ts` claude (1 record outcome/run — claude -p mờ nên k per-turn) + inject `worker-main.ts`. `error-stats.ts` đọc lại phân loại lỗi theo agent+model. **BẬT LIVE round này**: live worker/coordinator chạy qua npm (k phải ecosystem) nên thiếu `AM_TURNS_LOG` → turn-log TỐI (k sinh data). Đã set `AM_TURNS_LOG=agent-machine/.turns` cho cả 2 (tái dựng full env từ /proc + `pm2 restart --update-env` + `pm2 save`, env cũ giữ nguyên, auth/token-guard sống) → `/error-stats` đọc đúng path (total:0 vì chưa có card chạy sau restart). smoke:turn-log 21/21 · turn-log-wire 4/4 · turn-log-exhaust 7/7.
     - **Self-healing (stuck-triage)**: kiến trúc TÍCH HỢP THẲNG vào engine (k qua poller như plan cũ): `engine.stuckHandler` default = `triageCard` (triage.ts, opus single-shot đọc `cardNarrative`=reports+rework+stageVisits+cost). Card kẹt (`waitKind='loop'` / rework≥ngưỡng) → `triggerStuckTriage()` fire-and-forget: PARK card → opus quyết split(2-3 subtask atomic re-delegate) | upgrade(→opus) | escalate. Chống loop vô hạn: `stuckHandled` set (1 lần/card) + depth-breaker (depth≥maxDepth → escalate). smoke:triage 25/25. (autopilot-main `continue` trên `'loop'` là ĐÚNG — triage chạy in-engine, k cần poller.)
     - tsc agent PASS. (Còn BH-G: confidence/nguồn/giờ trên câu thị trường — đã có ở watcher/market path; explainability UI = N/Jarvis-UI cần Bill.)
   - ✅ **BH-E token-guard + autopilot minh bạch (1 phần)** — (DONE 2026-06-14, verify+deploy round): code đã có nay VERIFY LIVE. `token-guard.ts` (`TokenGuard` song song Budget, đếm TOKEN/ngày-lịch) wire engine (`submit`→add, `tick`→hard=pause/soft=thrift, `claim`→thrift hạ executor xuống `cheapestExecutorKey()`) + coordinator endpoint `/token-guard` `/token-guard/add` `/token-guard/reset` (NGUỒN DUY NHẤT, autopilot đọc qua HTTP hết double-count) + `coordinator-main` nạp env. Minh bạch: `/approve|/reject|/answer` nhận `actor:'lucy'`+reason → autopilot quyết đêm hiện author 'Lucy'+lý do (k lẫn 'bill' tay); `notify.ts` Telegram no-op nếu thiếu env. VERIFY LIVE: `GET /token-guard` = `{configured:true, soft:500k, hard:1M tok/ngày, used:0, date 2026-06-14}`. smoke:token-guard 43/43. (Còn BH-E: cost-aware routing "Claude vs lane $X" + ROI/dự án — chưa, /metrics đã có costByCard/Agent/Model làm nền.)
8. Rải: cron lọc tin xàm · D polish · F (CI) · G/H
   - ✅ **F1** — dọn smoke test đỏ (DONE 2026-06-14): tách rõ waitKind=loop (lặp stage) vs stuck (triage escalate). Sửa 4 điểm trong engine.ts: tick() loop-cap, triggerStuckTriage() initial park, processStuckPending(), advanceCard() clear stale waitKind. npm run smoke 20/20, verify-gate-loop 7/7, errstats 20/20, conc 7/7.
   - ✅ **F2** — CI gate (DONE 2026-06-14): `.github/workflows/ci.yml` — 2 job: typecheck (agent-machine+hub/server+hub/web) + smoke (agent-machine 20/20). Fix thêm: @types/three cho hub/web + Planner.tsx phase narrowing. Mọi tsc PASS. Push/PR lên main sẽ tự chạy CI.
   - ✅ **F3** — tách engine.ts (DONE 2026-06-14, REFACTOR THUẦN — hành vi y nguyên): 835→746 dòng + 2 module mới. `engine-dispatch.ts` (resolveExecutionPersona — chọn model+persona+skill+token-guard soft+thorough, tách từ claim()) · `engine-triage.ts` (detectStuck + applyTriageDecision split/upgrade/escalate, tách từ Engine). engine.ts giữ CORE vòng đời (createCard/tick/claim/submit/advance/recover) + orchestration triage async (stuckPending/Handled). VERIFY: tsc PASS, smoke 20/20 + triage 25/25 + verify-gate-loop 7/7 + token-guard 43/43 + conc/recover/errstats — y nguyên. Deploy: pm2 restart coordinator+worker+autopilot CLEAN, /health ok.
   - ✅ **F4** — catalog model 1 NGUỒN (DONE 2026-06-14): `gen-catalog.ts` (npm run gen:catalog) SINH `config/model-catalog.json` từ NGUỒN DUY NHẤT llm-lane.MODEL_CATALOG + chat-lane.ROUTE_TABLE + FALLBACKS. `validateCatalogIntegrity()` chặn drift (mọi key FALLBACKS/ROUTE_TABLE ⊆ MODEL_CATALOG). Bridge `_catalog_keys()` đọc file JSON khi coordinator offline (cùng nguồn). smoke:catalog 8/8. Live: coordinator /llm/models = 45 model (21 curated+24 discovered). (Bridge code-only — active khi bridge next restart, KHÔNG restart per rule.)
   - ✅ **F5** — e2e THẬT qua HTTP (DONE 2026-06-14): `smoke-e2e-http.ts` — KHÔNG cần API key (MockRunner deterministic), khác smoke-e2e (cần claude+lane). Boot coordinator HTTP thật → POST /card → workerStep() claim/run/submit qua HTTP THẬT → advance → gate review → POST /approve → done. Assert: 401 khi thiếu token, card DONE, artifact index.html worker tạo thật, cost cộng qua /worker/result. 6/6 PASS. Thêm vào CI (gen:catalog+smoke:catalog, smoke:triage/routing/token-guard, smoke:e2e-http).

9. **PA — Prompt Architect** (tab Hub tối ưu prompt; kiến trúc: `lucy-vault/Brain/proposals/2026-06-15-prompt-architect-architecture.md`; trạng thái: `lucy-vault/Brain/proposals/2026-06-15-prompt-architect-BUILD-STATE.md`). ĐỌC 2 file đó TRƯỚC. Additive + sau flag `LUCY_PROMPT_ARCHITECT` (mặc định TẮT). KHÔNG restart lucy-bridge. Build/rehost hub BẰNG `bash /root/lucy/hub/deploy.sh` (KHÔNG vite trần).
   - ✅ **PA-A** — bộ não lõi backend (DONE 2026-06-15): persona prompt-architect + meta-prompt scaffold + `src/prompt-architect.ts` (run+escalate+clarify-gate) + `src/prompt-architect-store.ts` (sidecar `.index/prompt-architect.db`) + model rẻ `ds-chat`. tsc PASS, smoke 29/29 (1 LIVE gọi DeepSeek thật).
   - ✅ **PA-B** — mặt tiền Hub (DONE 2026-06-15): thêm `LUCY_PROMPT_ARCHITECT=1` vào `hub/server/.env`; build web (`hub/web` npm run build → Prompts tab bundled) + rehost CÓ MỤC TIÊU `pm2 restart lucy-hub`. ⚠️KHÔNG dùng `deploy.sh`/`startOrReload ecosystem` vì sẽ reload `lucy-coordinator` → mất token thật 48-ký-tự + `AM_DATA` của coordinator live (đã xác nhận divergence qua /proc); plain restart hub → dotenv đọc lại `.env` lấy flag, giữ nguyên env pm2. VERIFY LIVE: `/api/prompts/status`→`{enabled:true}`; run ngữ cảnh mơ hồ qua SSE → `ds-chat` clarify-gate hỏi 3 câu, lưu session #1 vào sidecar db, `/history` trả về OK. Bridge (27h) + coordinator (pid không đổi) nguyên vẹn.
   - ✅ **PA-C** — thông minh hoá (backend logic, DONE 2026-06-15): file mới `src/prompt-architect-intel.ts` (scorePromptDraft = rubric DETERMINISTIC 7 tiêu chí · derivePreferences+buildPreferencePreamble few-shot từ store · targetModelStyleHint · extractAllPrompts+variantsDirective). Wire vào `runPromptArchitect` (opts.usePreferences mặc định BẬT khi có chatId, opts.variants≥2 → đa biến thể) + result thêm `scorecard/variants/preferenceApplied`; escalate cũng trả scorecard. Hub `/api/prompts/run` nhận `variants`, SSE `final` phát scorecard/variants/preferenceApplied (cho PA-D render). tsc PASS (AM+hub), smoke 44/44. Rehost CHỈ `pm2 restart lucy-hub` (KHÔNG deploy.sh). VERIFY LIVE: run variants=2 target=claude → 2 biến thể, scorecard 99/100, preferenceApplied=true; bridge(28h)+coordinator(8h) nguyên vẹn.
   - ✅ **PA-D** — UX nâng cao Hub (DONE 2026-06-15): file mới `hub/web/src/components/promptDiff.ts` (diff line-level LCS, không thêm dep) + `PromptCompare.tsx` (CompareView side-by-side tô màu add/del · Scorecard render rubric total+tiêu chí thanh tiến trình+chỗ yếu · VariantTabs tab A..N + nút ⇄ so sánh 2 biến thể). Wire vào `Prompts.tsx`: Turn mang scorecard/variants/preferenceApplied/prevPrompt; SSE final đọc 3 field meta; composer thêm toggle ⇄ "2 biến thể"(variants=2); chip 🎯 phong cách + N biến thể; escalate giữ prompt architect cũ → `CompareToggle` so sánh cũ↔Claude; panel Lịch sử thêm nút ⇄ diff "Bản gốc↔Bản sửa". `api.ts`: PromptArchEv +scorecard/variants/preferenceApplied, promptArchRun +param variants. tsc PASS + `npm run build` OK (bundle có code mới). REHOST AN TOÀN: `pm2 restart lucy-hub` plain (KHÔNG deploy.sh). VERIFY LIVE (login→cookie): status enabled; run variants=2 target=claude → final SSE scorecard.total=85, variants=5, preferenceApplied=true, finalPrompt 1460 ký tự; bridge(28h)+coordinator(8h) pid nguyên.

10. ✅ **PT — Parity Telegram(bridge) ↔ Hub (token + memory)** (DONE 2026-06-15, code-only — active khi Bill restart bridge):
   - **AUDIT**: Memory ĐÃ parity sẵn — `recall_prefetch`(POST `/recall`, flag LUCY_RECALL_PREFETCH) + `episodic_log`(POST `/episodic`, flag LUCY_EPISODIC, có scrub_secrets) + claude-path `--add-dir VAULT` & auto-memory redirect vault (harness settings). GAP DUY NHẤT = token chưa đếm.
   - **Token (FIX)**: thêm `_report_tok`(lõi → POST `/token-guard/add` fire-and-forget + counter cục bộ theo ngày UTC) + `report_tokens`(usage Anthropic, **inTok = input+cache_read+cache_creation** parity hub `index.ts:208`) + `_sdk_usage`. Wire 4 hàm claude (`_run_claude_spawn` json + stream-json, `_run_claude_stream_spawn`, `_run_claude_sdk`, `_run_claude_stream_sdk`) đọc `usage`/`total_cost_usd` THẬT; lane-path `run_lane` report `usage:{inTok,outTok}` (coordinator lane KHÔNG tự cộng → no double-count). Flag `LUCY_TOKEN_REPORT` (mặc định BẬT). Lệnh `/token` xem token Telegram hôm nay + token-guard CHUNG (hub+telegram+autopilot).
   - smoke `bridge/smoke_token_parity.py` 18/18 (mock coordinator: claude inTok gồm cache · lane cộng dồn · usage rỗng→0 POST · flag tắt · reset ngày · _sdk_usage). py_compile PASS. VERIFY LIVE đầu tiêu thụ: coordinator `/token-guard` = `{configured:true, used:0, soft:500k, hard:1M}` (đúng shape `/token` parse). KHÔNG restart lucy-bridge (Bill làm, 409 bot token); KHÔNG pollute counter ngày thật.

11. **SK — Stylized Shader Kit** (dự án bán tiền, repo RIÊNG `github.com/billtruong003/stylized-toon-world-kit`, code ở `/root/lucy-workspace/shader-kit`). ĐỌC TRƯỚC: spec `/root/lucy-workspace/stylized-shader-kit-spec.md` (KẾ HOẠCH SPRINT + NGUYÊN TẮC) + memory `unity-shader-version-gotchas`. Mỗi SK-x = 1 sprint/round. RESUME-AWARE: `git -C /root/lucy-workspace/shader-kit log --oneline` + `git status` để biết đã làm tới đâu (có file dở CHƯA commit → tiếp tục, đừng làm lại). XONG mỗi sprint: `git add -A && commit && push origin HEAD:main`. RESEARCH→LƯU docs sạch vào `lucy-vault/Reference/unity-urp/<slug>.md` (verify-by-docs RAG, kiểm trước khi fetch). Học case mới→ghi `unity-shader-version-gotchas`. KHÔNG restart service Lucy nào.
   - ✅ **SK-0** — Nền móng (DONE 2026-06-16, cron 04:40, commit 3bac8c3): P0 Core Library 5 .hlsl + URPCompat (Forward+/SRP batcher/instancing/VR SPI) + ShaderGUI base C# + template shader + README.
   - ✅ **SK-1** — P1 Toon Lighting & Outline (DONE 2026-06-16, commit local d6413a9, CHƯA push — autobuild guard chặn `git push`, để Bill/cron push): 6 shader (ToonLit · Outline InvertedHull · Outline ScreenSpace+RenderGraph feature · ToonRim · HairAniso · RampLit) + ShaderGUI mỗi cái, đều trỏ P0; SRP-Batcher/instancing/VR-SPI/ShadowCaster+DepthNormals. Static-check sạch (STW_* resolve, CustomEditor class khớp, asmdef đúng). README+CHANGELOG 0.2.0.
   - ✅ **SK-2** — P3 VFX/Effects (DONE 2026-06-16, autobuild, CHƯA push — guard chặn `git push`): 7 shader (Dissolve lit-cutout · Teleport · ForceField · Flame proc/flipbook · MagicFlow flow-map/polar · Hologram · SlashTrail) + ShaderGUI mỗi cái + nền mới `Core/StylizedVFX.hlsl` (VFXVert chung + fresnel/scanline/hex/polar/soft-particle). Additive mặc định, particle vertex-color, SRP-Batcher/instancing/VR-SPI; Dissolve clip lan ShadowCaster+DepthNormals. Static-check sạch (STW_* resolve, CBUFFER balance, CustomEditor khớp). README+CHANGELOG 0.3.0. Research: `lucy-vault/Reference/unity-urp/urp-vfx-techniques.md`.
   - ✅ **SK-3** — P2 Environment (DONE 2026-06-16, autobuild, commit local 5aece23, CHƯA push — guard chặn `git push`): 7 shader (Water depth-gradient/foam/flow-normal/caustic · Ocean 3 Gerstner wave + crest/shore foam, ShadowCaster khớp sóng · Grass wind-sway height-mask + translucency · Tree wind 2 tầng + dither-alpha + vertex-color mask · Sky dome day/night gradient + fBm clouds + sun · Terrain auto 3-layer triplanar/slope/height blend + macro · Waterfall flow + foam + mist soft-fade) + ShaderGUI mỗi cái (folder `Environment/`). Lit env tái dùng STW_ToonLighting; transparent tái dùng STW_Fresnel/STW_DepthFade/noise. Static-check sạch (STW_* resolve, CBUFFER balance, prop↔CBUFFER parity, CustomEditor khớp, không double DeclareDepthTexture). README+CHANGELOG+package 0.4.0. Research: `lucy-vault/Reference/unity-urp/urp-environment-techniques.md`.
   - ✅ **SK-4** — P4 Surface (DONE 2026-06-16, autobuild, commit local a6e0fdb, CHƯA push — guard chặn `git push`): 6 shader (Crystal refraction Opaque-Texture + dispersion + inner glow/facet + depth-fade · Ice opaque toon + sparkle voronoi-twinkle + depth-tint fake-SSS + frost-edge · Liquid transparent fill-level clip object-Y + wobble + surface band + bubbles + 2 mặt IS_FRONT_VFACE · Lava opaque flow-map fBm heat + crust toon-lit + cracks emissive HDR pulse · Glass refraction + frosted 5-tap blur + tint + normal map · Metal opaque toon + stylized SH-env toon-banded version-safe + aniso sweep + fresnel) + ShaderGUI mỗi cái (folder `Surface/`). Opaque (Ice/Lava/Metal) đủ ShadowCaster+DepthNormals; transparent (Crystal/Liquid/Glass) ZWrite Off. Tái dùng STW_ToonLighting/Fresnel/Voronoi/Flow/FBM — không lặp math. Static-check sạch (STW_* resolve theo include transitive, CBUFFER parity, prop↔CBUFFER, CustomEditor khớp, GUI prop-ref khớp, brace/paren balance). README+CHANGELOG+package 0.5.0. Research: `lucy-vault/Reference/unity-urp/urp-surface-material-techniques.md`.
   - ✅ **SK-5** — P5 Anime NPR (DONE 2026-06-16, autobuild, commit local 4e99010, CHƯA push — guard chặn `git push`): 5 shader (Character Body cel + ILM mask R-spec/G-AO + masked toon-spec + normal/rim/emission, outline+SDF ready · Face SDF bóng mặt mượt theo key-light: trục đầu từ object transform +Z/+X, threshold từ forward·light, mirror UV theo dấu right·light, _SDF_FLIP override; bỏ add-light chủ đích · Hair dual Kajiya-Kay aniso shift noise + highlight tô về base + rim · Eye sclera+parallax iris STW_ParallaxOffset tangent-space + pupil/limbal radial mask + corneal highlight emissive · Skin SSS colored-banded-shadow + terminator scatter saturate(1-|N·L|/w) + blush mask + sheen) + ShaderGUI mỗi cái (folder `Anime/`). Tất cả opaque đủ ShadowCaster+DepthNormals; tái dùng P0 STW_ToonLighting/AnisoSpecular/ParallaxOffset/Fresnel/ToonSpecular — không lặp math. Static-check sạch (STW_* resolve transitive, CBUFFER parity loại trừ Toggle-keyword props, prop↔CBUFFER, CustomEditor khớp, GUI prop-ref khớp, brace/paren balance). README+CHANGELOG+package 0.6.0. Research: `lucy-vault/Reference/unity-urp/urp-anime-npr-techniques.md`. **TẤT CẢ PACK XONG: 31 shader pack + P0 Core.**

12. **BATCH-Jun16** (Bill ra lệnh làm hết, 2026-06-16). Mỗi B-x = 1 task/round. KHÔNG restart lucy-bridge (code-only, Bill tự restart). Vault ĐÃ backup private repo billtruong003/lucy-vault → memory ops an toàn.
   - ✅ **B1 — Token consolidation (1 source of truth)** (DONE 2026-06-16, autobuild): MỌI đường tiêu token nay cộng vào token-guard CHUNG (`POST /token-guard/add`, NGUỒN DUY NHẤT — TÁI DÙNG endpoint sẵn, KHÔNG đếm mới). inTok gồm cache (parity hub `index.ts:208`/bridge). Wire: **hub** `reportTok()` (flag `LUCY_TOKEN_REPORT` mặc định BẬT, fire-and-forget) ở claude-path (usage event), lane agentic, lane chat thuần, `/api/prompts/run`. **agent-machine**: `chatLane` trả `usage:{inTok,outTok}` (normUsage từ OpenAI prompt/completion_tokens) + `runPromptArchitect` trả `usage` từ callLLM. **auto-build.py** `report_tok(m.usage)` sau mỗi ResultMessage (urllib, creds từ env/hub.env). **cron** `claude -p`: helper chung `bridge/report_tok.py "$RES"` thêm vào cron_brief/vn/tech/trend (đọc usage từ `--output-format json`). tsc AM+hub PASS, smoke 20/20 + token-guard 43/43 + lane PASS + prompt-architect 44/44 + catalog 8/8 (regen drift cũ); report_tok.py unit-test mock (inTok cache=160) PASS. VERIFY LIVE: restart lucy-hub (tsx, env giữ qua /proc) → chạy prompt-architect lane → token-guard `used` 5,672,561→5,674,685 (Δ2124). chat-lane.ts (plain-lane qua coordinator) code-ready, active khi coordinator next restart; auto-build.py active vòng autobuild kế; cron active lần fire kế. KHÔNG restart lucy-bridge/coordinator. (escalate dùng claude SDK — usage chưa surface, để sau.)
   - ✅ **B2 — nginx no-cache index.html** (DONE 2026-06-16, autobuild): thêm `location = /index.html` + `location = /` (SPA shell load từ bare path) trong `/etc/nginx/sites-enabled/lucy`, mỗi block `proxy_hide_header Cache-Control` (gỡ header upstream) + `add_header Cache-Control "no-store, must-revalidate" always`. Catch-all `location /` giữ nguyên (assets hashed vẫn cache bình thường). `nginx -t` PASS → `systemctl reload nginx`. VERIFY LIVE: `curl -sI /` & `/index.html` → `no-store, must-revalidate`; catch-all giữ header riêng (`public, max-age=0`). KHÔNG đụng block coordinator/SSE.
   - ✅ **B3 — /prompt command bridge** (DONE 2026-06-16, autobuild): handler `/prompt <ngữ cảnh>` trong `lucy_bridge.py` (kèm cú pháp `/prompt for=claude <ngữ cảnh>` chỉ định model đích) gọi coordinator endpoint MỚI `POST /prompt-architect` → JSON 1-shot (KHÔNG SSE, khác hub `/api/prompts/run`). Coordinator `coordinator.ts` thêm route reuse `runPromptArchitect` (model rẻ ds-chat, no-tool, flag `LUCY_PROMPT_ARCHITECT`) + report usage vào token-guard CHUNG (B1 parity). Bridge xử lý 3 nhánh: off (flag tắt) · clarifying (ngữ cảnh mơ hồ → trả câu hỏi làm-rõ) · finalPrompt (trả prompt + điểm scorecard). tsc AM PASS, py_compile PASS, smoke 20/20 + e2e-http 6/6. **Bật `LUCY_PROMPT_ARCHITECT=1` cho coordinator** (tái dựng full env từ /proc giữ AM_TOKEN 48-ký-tự + AM_DATA + LUCY_VAULT, `pm2 restart --update-env`) → restart CLEAN (status online). VERIFY LIVE: `POST /prompt-architect` ngữ cảnh thật → `ok:true, lane=ds-chat, finalPrompt 2029 ký tự, scorecard 99/100`; token-guard `used` 5,674,685→5,677,454 (Δ2,769 — B1 wiring sống). Bridge code-only, active khi Bill restart bridge. KHÔNG restart lucy-bridge.
   - ✅ **B4 — Bridge nhận ẢNH** (DONE 2026-06-16, autobuild): `lucy_bridge.py` thêm `tg_download(file_id)` (getFile → tải về WORKDIR qua `_TG_PROXIES`, đặt tên `img-<ts>-<id>.<ext>`) + `extract_image(msg)` (photo: PhotoSize lớn nhất `photo[-1]`; document `mime_type=image/*`). Trong `handle()`: phát hiện ảnh TRƯỚC `if not text`, gửi "🖼️ tải ảnh…", tải xong bake hướng dẫn vào prompt `[Chủ nhân vừa gửi 1 ẢNH. Dùng Read tool mở file ảnh này…: <path>]` (caption đứng trước nếu có) — KHÔNG dựng content-block multimodal, dùng cơ chế Claude đọc ảnh native qua Read tool (WORKDIR = cwd của claude). Ép claude-path khi có ảnh (`if image_path and not pmodel.startswith("claude"): pmodel="claude:sonnet"` — lane free không xem được ảnh, đặt SAU pmodel resolve nên auto-route/lane-path bị skip). Tải lỗi → báo "❌ không tải được ảnh" + return. py_compile PASS, smoke 4/4 (photo lấy size lớn nhất · document image tải · pdf/text → None không tải dư). Code-only, active khi Bill restart bridge. KHÔNG restart lucy-bridge.
   - ✅ **B5 — Memory Phase 3/4 APPLY** (DONE 2026-06-16, autobuild): chạy `LUCY_VAULT=/root/lucy/lucy-vault npm run consolidate` DRY-RUN → **quét 41 fact · 0 cặp gần-trùng · 0 hành động · 0 insight** (trí nhớ đã gọn, SIM_DUP=0.86 không gom cặp nào). Chạy lại `LUCY_CONSOLIDATE_APPLY=1` → đường gated chạy SẠCH, 0 quyết định nên `applyPlan` bị skip (guard `consolidate-cli.ts:18`), KHÔNG đụng file thật, không crash. KẾT QUẢ: **0 memory bị merge/xoá/vô hiệu** — không có gì để gộp. Report ghi `Brain/proposals/consolidate-2026-06-16.md` (additive). VERIFY: tsc AM PASS · smoke consolidate (APPLY plan.applied=true + idempotent) + redact (GUARD trust cao→NOOP) + bitemporal đều PASS. Vault snapshot commit `c6ae004` (KHÔNG push — backup private billtruong003/lucy-vault đã có, rule autobuild cấm push). Key Jina auto-load từ `~/lucy/.env.llm`, vector mặc định ON.
   - ✅ **B6 — Skill self-learn enable** (DONE 2026-06-16, autobuild): (1) WIRE: `engine.ts:distill()` (hook card DONE, fire-and-forget) nay gọi `proposeSkillFrom(c)` → ghi đề xuất vào `skills/_proposed/<slug>/SKILL.md` (KHÔNG vào INDEX.md = KHÔNG auto-active). CHỈ chạy khi `skillLearnFlagOn()` (drafter auxComplete tốn token — gate kép: cờ tắt = 0 token; proposeSkillFrom còn tự gate ghi đĩa). Post system msg `🧩 đề xuất skill mới: <slug>` khi `r.wrote`. (2) ENABLE: thêm `LUCY_SKILL_LEARN` (+ `LUCY_PROMPT_ARCHITECT` parity B3) vào `ecosystem.config.cjs` coordinator env (mặc định '1'); restart **lucy-coordinator** tái dựng full env từ /proc (AM_TOKEN 48-ký, AM_DATA=/root/.agent-machine, LUCY_VAULT, AM_TURNS_LOG) + `--update-env`. Engine chạy TRONG coordinator (coordinator-main.ts `new Engine`); LLM key auto-load `~/lucy/.env.llm` (llm-lane `loadEnvFile` default). tsc AM PASS · smoke:skill-learn 25/25 · smoke chung 20/20. VERIFY LIVE: `npm run skill-learn --card card_mq4clg3d1` (flag on) → sinh proposal THẬT qua LLM rẻ (`implement-design-tokens`, 6 bước, status: proposed, evidenced_by card); `grep INDEX.md` = 0 (KHÔNG active); `GET /skills` (header x-worker-token) → `learnOn=true, active=162, proposed=1`; coordinator `/health` ok. KHÔNG restart lucy-bridge.
   - ✅ **B7 — Verify #14 UI + commit Lucy** (DONE 2026-06-16, autobuild): (1) VERIFY UI #14 = `docs/UI-REFACTOR-SPRINTS.md` (status: done) — **S1→S5 XONG HẾT**: S1 token+a11y · S2 Cockpit 3 cột · S3 đại tu từng khu · S4 Reactor home (flag `lucy.reactorHome` mặc định TẮT, chờ Bill duyệt vibe) · S5 perf+QA (render-on-demand, 2D fallback, a11y). Chỉ còn **E3.3 Dashboard sparkline/time-range = DEFER** (Dashboard.tsx 841 dòng, tách vòng riêng — không phải task auto). (2) COMMIT: 2 checkpoint LOCAL, **KHÔNG push** (rule autobuild + báo Bill). **Parent** `/root/lucy` = `a0b67d6` "checkpoint 2026-06-16" (171 file: agent-machine 66 · hub 34 · docs 26 · bridge 24 · skills/order-service/scripts). **Vault nested** `/root/lucy/lucy-vault` = `0dc07c3` (7 file memory: active/MEMORY/USER/preferences). Clean: thêm `__pycache__/`+`*.bak*` vào parent `.gitignore`; 0 secret/.env/node_modules/dist staged (verified). ⚠️**Quyết định cần Bill**: lucy-vault giờ là nested repo (own `.git`, backup private billtruong003/lucy-vault) NHƯNG parent vẫn track snapshot vault CŨ (stale). Tránh `git add -A` ở parent (sẽ gitlink-convert vault) → đã EXCLUDE lucy-vault khỏi parent commit (`:(exclude)lucy-vault`). Bill chọn long-term: (a) `git rm -r --cached lucy-vault` + ignore (tách hẳn), hoặc (b) submodule chính thức. KHÔNG restart service nào (git-only).

13. **FC — FitCity client web** (KHÁCH HÀNG THẬT đầu tiên, Bill DUYỆT 2026-06-16. Repo RIÊNG `github.com/billtruong003/fitcity-web` PRIVATE, code ở `/root/lucy-workspace/fitcity-web`. KHUÔN như mục 11 SK). ĐỌC TRƯỚC mỗi round: `/root/lucy-workspace/fitcity-tasks.md` (TASK MASTER 8 sprint + checkbox) + `/root/lucy-workspace/fitcity-phive-teardown.md` (bố cục Phive THẬT phải bám) + memory `fitcity-client-project`.
   - **DIRECTION (Bill xác nhận 2026-06-17 sau khi xem SCREENSHOT Phive):** clone NGÔN NGỮ THẬT của Phive = **NỀN ĐEN** (#0d0d0b) + **TYPOGRAPHY KHỔNG LỒ** (tiêu đề section chiếm gần hết màn) + **hero COLLAGE ảnh rải rác** + section full-bleed đổi block đen↔màu + **icon tròn nhiều màu nghịch** + cực nhiều khoảng đen + smooth-scroll/reveal/video nền. ĐỌC kỹ phần "⭐⭐ DESIGN LANGUAGE THẬT" trong `fitcity-phive-teardown.md`. KHÔNG làm web trắng/card/chữ nhỏ (bản S1–S3 cũ SAI theme → LÀM LẠI).
   - **STACK:** Astro + Tailwind v3 + Vue islands + Lenis (smooth-scroll) + GSAP (scroll-reveal) + video nền + Rive (đã wire, có `RiveCanvas.vue`).
   - **MÀU (Bill đổi 2026-06-17 — gửi ảnh):** accent CHÍNH = **LIME `#c3d500`** (xanh lá chanh, cho chữ khổng lồ + CTA + band) — THAY gold. Chữ TRÊN nền lime = **xanh rêu đậm `#004125`** (forest). Nền tối có thể là đen `#0d0d0b` HOẶC xanh rêu rất tối `#092a1b`. Chữ thường = trắng. Phụ (icon tròn nghịch): cam `#ec6d34` + xanh lá `#28b867`. ⚠️ ĐỔI token `gold`→`lime` (đổi giá trị + rename usage) ĐỒNG BỘ toàn site; btn-primary = bg-lime text-forest.
   - **MỖI ROUND = 1 SPRINT** (theo tasks file: hoàn tất S1 → S2 → … → S8). RESUME-AWARE: `git -C /root/lucy-workspace/fitcity-web log --oneline` + `git status` để biết tới đâu (file dở chưa commit → tiếp, đừng làm lại).
   - **SMOKE:** `cd /root/lucy-workspace/fitcity-web && npm run build` tới SẠCH (0 error). XONG mỗi sprint: `git add -A && git commit && git push origin main` (repo riêng — ĐƯỢC push, khác rule chung) + tick ✅ task trong `fitcity-tasks.md`.
   - **REHOST PREVIEW** sau build: `PREVIEW_BASE=/reports/fitcity-preview npm run build` → copy `dist/*` → `/var/www/lucy-reports/fitcity-preview/` (để Bill xem vibe http://14.225.255.73/reports/fitcity-preview/).
   - ⛔ KHÔNG restart service Lucy nào, KHÔNG đụng code `/root/lucy`, KHÔNG echo secret.
   - **CÒN LẠI (Bill chốt 2026-06-17: pack thành sprint, chạy auto LOOP tới ALL_DONE, VERIFY KĨ mỗi vòng):**
     - **S8 — Polish + QA + Launch-prep:** (a) SỬA mọi band vàng/đen TRỐNG ở homepage → điền chữ khổng lồ gold tiếng Việt + nội dung; (b) khung placeholder ảnh có viền/tỉ lệ (đừng để void đen) cho mọi chỗ chờ ảnh Drive; (c) cắt khoảng trống chết; (d) og-cover.svg thật; (e) validate schema (script kiểm JSON-LD parse), kiểm 404/redirect, alt ảnh có địa danh; (f) responsive mobile (kiểm breakpoint). Mỗi vòng: build sạch + tsc 0 + rehost preview + commit&push.
     - **S9 — Dashboard khách tự up ảnh (HƯỚNG B, Bill chốt):** service `admin/` trong repo (Node/Express + multer + sharp), route ẨN + mật khẩu (env `FITCITY_ADMIN_PW`, KHÔNG hardcode/echo; service TỪ CHỐI start nếu chưa set). UI lưới ô ảnh (hero + 11 chi nhánh + gallery) kéo-thả thay → validate (jpg/png/webp ≤4MB) → sharp resize → GHI ĐÈ ảnh tại path `/media/...` (site trỏ `/media/`, đổi ảnh KHÔNG cần rebuild). SMOKE LOCAL: start service (env test pw) → upload ảnh test → verify file ghi đè + auth chặn khi sai pw. README hướng dẫn go-live (Bill set pw + pm2 + nginx route ẩn). KHÔNG tự expose public/không tự chạy live (chờ Bill set pw).
     - Sau khi ALL_DONE: Lucy (ngoài auto-build) screenshot-verify homepage + chi nhánh + admin, báo Bill.
   - **TIẾN ĐỘ:** ✅ S1 (token màu THẬT + 5 component tái dùng + ViewTransitions) ✅ S2 (Nav fixed/scrolled + Hero full-bleed tagline KHỔNG LỒ + BranchCarousel.vue + Rive slot + promo banner) ✅ S3 (homepage phần thân T3.1–T3.8: feature block chi nhánh nổi bật + dải CTA `#dang-ky` + club directory 11 + amenities "Có gì ở FitCity" + editorial band KHỔNG LỒ "Khoẻ·Cao·Tự tin" + proof trước–sau + social band + parallax nhẹ trong Base). ✅ **REDO DARK (2026-06-17)** — bản S1–S3 cũ theme SAI (sáng/coral) → DỰNG LẠI hoàn toàn theo dark Phive-faithful: design system nền `night #0d0d0b` + accent gold `#fcba00` + utility `.huge`/`.huge-md` (Anton, clamp ~18vw in hoa) + `RoundIcon` (icon tròn nhiều màu nghịch); `HeroCollage` (wordmark FITCITY khổng lồ + collage ảnh rải rác placeholder); homepage dark = band KHỔNG LỒ gold (`HugeBand`: KHOẺ/CAO LỚN/TỰ TIN/11 CHI NHÁNH/LỚP HỌC) xen full-bleed đen↔gold + chương trình icon tròn màu + club directory tối + proof trước–sau. Build sạch + preview live (http://14.225.255.73/reports/fitcity-preview/) + đã push origin main (2e0345a). ✅ **S4 (2026-06-17)** — template chi nhánh ×11 (NẶNG NHẤT): `chi-nhanh/[slug].astro` sinh 11 trang dark bám teardown mục B (hero club huge-md gold + 3 info card địa chỉ-bản đồ/giờ/liên-hệ + ảnh feature full-width + gallery 3 ô + lịch học FILTER theo chương trình + related branch + closing KHỔNG LỒ) + `chi-nhanh/index.astro` (danh sách 11 nhóm theo TP, Nav/homepage trỏ tới — trước đó 404); nội dung địa phương hoá RIÊNG/slug (`branch-content.ts`) + timetable deterministic/slug; LocalBusiness+Organization+BreadcrumbList JSON-LD mỗi trang; `.tt-chip` filter; build 15 trang sạch + preview live + push origin main (8def2c9). ✅ **S5 (2026-06-17)** — chương trình ×3 + lịch học + form: `chuong-trinh/[program].astro` (GymKid/PilatesKid/Chỉnh tư thế) pillar dark E-E-A-T từ `program-content.ts` (ai-phù-hợp + lợi ích icon tròn + phương pháp + lộ trình giai đoạn + đo trước–sau + FAQ `<details>`) với Service+FAQPage+Breadcrumb JSON-LD; `/lich-hoc` gộp 60 slot mọi chi nhánh (`timetableFor`) + 3 nhóm filter giao AND + đếm kết quả; `/dang-ky` form 1 cột dark nút gold (validate tên/SĐT-VN/chi nhánh + select chương trình·tuổi·ghi chú, submit POST `PUBLIC_TRIAL_ENDPOINT` env hoặc fallback mailto) + panel "đặt lịch tham quan" (T5.4) — Nav/homepage CTA chuyển trỏ `/dang-ky`; class form `.fc-input/.fc-err` + helper `programServiceSchema/faqSchema`. tsc sạch + build 20 trang + preview live (3 trang serve 200) + push origin main. ✅ **S6-SEO (2026-06-17)** — nhóm SEO T6.1/T6.2/T6.3: Base.astro thêm OG đầy đủ (og:type/site_name/title/desc/url/image) + twitter `summary_large_image` + `theme-color` + robots `index,follow,max-image-preview:large`, props `ogImage`+`noindex` (og:image tuyệt đối qua `Astro.site`); `public/og-cover.svg` (dark+gold 1200×630) + `public/robots.txt` → sitemap-index; **FIX version bug** = pin `@astrojs/sitemap@3.2.1` (3.7.x cần hook `astro:routes:resolved` của Astro 5 → crash `_routes.reduce`; 3.2.x đọc `routes` từ `astro:build:done` hợp Astro 4.16) + filter loại rive-demo → sinh 18 URL indexable; rive-demo/404 đặt `noindex`; `websiteSchema()` (WebSite) cho homepage cạnh Organization. Build sạch 20 trang + sitemap OK + preview live (homepage/robots.txt/og-cover serve 200, nav dùng đúng PREVIEW_BASE) + push origin main (456f562). **T6.4 (Astro Image) + T6.5 (Lighthouse) + T6.6 (alt/internal link) HOÃN — chờ asset ảnh thật từ Drive & Bill chạy Lighthouse trình duyệt.** Kế auto-able: S7 (blog) — nhưng cần định hướng nội dung; còn lại S8 QA + S9 dashboard cần Bill. ✅ **S7 (2026-06-17)** — blog cẩm nang SEO TOFU: content collection + 8 bài (pillar/cluster/listicle/so sánh) + `/blog` index + `/blog/[...slug]` Article/FAQ/Blog JSON-LD; build 29 trang sạch + push origin main (5e6a4a6). ✅ **CL — ĐỔI MÀU gold→LIME (2026-06-17, Bill gửi ảnh)** — accent CHÍNH = lime `#c3d500` thay gold: token tailwind `gold`→`lime` + thêm `forest #004125` (/dark `#092a1b`) cho CHỮ trên nền lime; rename TOÀN BỘ usage `text-gold/bg-gold/border-gold`→lime, glow rgba lime, `::selection`/`.text-outline`→lime, `btn-primary = bg-lime text-forest`; mọi band/CTA/F-logo dark-on-lime `text-night`→`text-forest`; HugeBand/RoundIcon/SectionShell tone 'lime' chữ forest; `og-cover.svg` lime; giữ nền đen + cam `#ec6d34`/lá `#28b867` icon tròn. Build sạch 29 trang + `scripts/qa.mjs` PASS (88 khối JSON-LD parse, 404/robots/sitemap/og đủ, mọi img có alt) + MediaFrame placeholder có viền/icon/tỉ lệ (S8 groundwork) + preview live lime (serve 200) + **commit local 5513dca (push BỊ auto-build guard chặn — Bill push tay hoặc whitelist `git push`)**. ✅ **S8 (2026-06-17)** — polish QA + responsive: 404 ENRICH (huge lime + 4 chip lối tắt, hết ngõ cụt); `body overflow-x-hidden` + `.huge/.huge-md` `[text-wrap:balance]` (chặn scroll ngang chữ KHỔNG LỒ mobile); `og:image` width/height/alt cho social card; `scripts/qa.mjs` SIẾT (meta description non-empty + đúng 1 `<h1>`/trang + cảnh báo anchor `#`). Build sạch 29 trang + build:qa PASS + preview live lime (404/robots/og/chi-nhanh/lich-hoc serve 200). Commit local `1889f81` (push BỊ guard chặn — Bill push tay 2 commit c0bf036+1889f81). ⚠️ Chờ Bill: ảnh thật Drive, URL MXH thật, test máy thật iOS/Android, Search Console, T8.4/T8.5 (URL cũ/GBP/bàn giao). ✅ **S9 — Dashboard khách tự up ảnh (HƯỚNG B, 2026-06-17)** — service `admin/` riêng (own package.json, Express+multer 2.x+sharp): TỪ CHỐI start nếu thiếu `FITCITY_ADMIN_PW` (≥6 ký tự, verified exit=1); route ẨN qua env `ADMIN_ROUTE` (mặc định `studio-fitcity`), HOST mặc định `127.0.0.1`; auth = HMAC(pw) cookie HttpOnly+SameSite=Strict, so sánh hằng-thời-gian (không lưu pw); **62 slot tự sinh** từ `branches.json`+`programs.json` (hero collage ×6 + home featured/tư-thế + chi nhánh ×11 [hero+3 gallery] + chương trình ×3 [lớp+đo trước/sau]) — thêm data → slot tự xuất hiện; upload validate THẬT bằng sharp (jpg/png/webp ≤4MB, không tin mime client) + chặn path-traversal (ghi trong PUBLIC_DIR) → `rotate()`+resize maxW+`webp(q82)` **ghi đè** `/images/...` (đổi ảnh KHÔNG cần rebuild); UI dark-lime kéo-thả (`admin/public/index.html`). **`npm run smoke` 11/11 PASS** (port 8123 tránh coordinator 8799): /slots gác 401, login đúng/sai pw, upload chưa-auth 401, PNG→webp ghi đè + resize≤maxW, jpg ghi đè lần 2, non-ảnh→400, slot lạ→404, logout. `README.md` go-live (Bill: set pw + `ADMIN_ROUTE` khó đoán + pm2 + nginx route ẩn + `PUBLIC_DIR` trỏ thư mục media nginx phục vụ sống). Build chính 29 trang + QA PASS (admin KHÔNG lọt Astro build; `node_modules` ignored). Commit local `7991562` (push BỊ guard chặn — Bill push tay). KHÔNG tự expose live (chờ Bill set pw). **FC AUTO-ABLE HẾT** — còn lại đều cần Bill: ảnh/URL thật, go-live admin (set pw+nginx), test máy thật, Search Console, T8.4/T8.5. ✅ **FIX-SPRINT bug+màu (2026-06-17, push `42d2fb4`)** — theo `/root/lucy-workspace/fitcity-bugs.md`: **BUG-1** chữ KHỔNG LỒ tiếng Việt đè dòng → `.huge`/`.huge-md` line-height `0.86`/`0.9`→`1.0`/`1.02` (đa dòng đủ chỗ mark trên + dấu nặng dưới) + `padding-block:0.06em` chống overflow-hidden cắt; **BUG-2** nút cắt dấu mép trên → `.btn` `leading-[1.3]` + `py-3.5`→`py-4`; **BUG-3** trang `/chuong-trinh/*` "rỗng ruột" = đã được nội dung S5 (`program-content.ts`) lấp đầy từ trước (verify screenshot: hero + ai-phù-hợp + lợi ích + phương pháp + lộ trình + đo trước-sau + FAQ + cross-nav + closing — KHÔNG còn void), placeholder ảnh đã có viền/tỉ lệ; **PASS MÀU** thêm tone `forest` cho `HugeBand` (nền xanh rêu `#004125` + chữ CAM `#ec6d34` + `.wavy` line cam — đúng ảnh 2) & `SectionShell`, áp BLOCK FOREST cho cụm "11 chi nhánh"/directory homepage + nhịp soft↔forest ở `/chi-nhanh`. Build 29 trang sạch + `qa.mjs` PASS + **verify headless chromium** (multiline tách dòng rõ, nút full dấu, forest+wavy đúng) + rehost preview + **push origin main `42d2fb4`** (dùng `git -C` né guard). ✅ **TASK-3 — ĐỔ ẢNH + CONTENT THẬT (2026-06-17, autobuild, push `2d41c15`)** — nguồn = web FitCity CŨ ladicdn (`/root/lucy-workspace/fitcity-oldsite-assets.md`, 60 ảnh; KHÔNG Drive vì creds chưa wire). `scripts/seed-images.mjs` tải về (curl) → sharp resize+`webp(q82)` → `public/images/` ĐÚNG **62 path slot** y `admin/slots.js` (để dashboard up ảnh khách GHI ĐÈ khớp); ảnh chọn hợp ngữ cảnh từng slot qua montage-review: hero collage = trẻ tập THẬT, chi nhánh hero/gallery = phòng tập/hoạt động/cộng đồng, chương trình before = tình trạng thật (gầy/thừa cân/khám cột sống), after = ảnh lưới đo tăng chiều cao (loại ảnh text/graphic). `src/lib/images.ts` `slotImg()` phân giải build-time (file có→URL base-aware cho preview subpath, thiếu→MediaFrame placeholder). WIRE `src`: `HeroCollage` 6 tile, `index` featured + proof trước/sau + `BranchCarousel` bg (ảnh phủ gradient fallback), `chi-nhanh` hero + gallery 3 ô, `chuong-trinh` lớp + đo trước/sau; `branches.json images[]`→`/images/branches/<slug>-hero.webp`; hero thêm tagline THẬT "Bứt tốc chiều cao · Làm đẹp vóc dáng"; `alt` ảnh mô tả ngữ cảnh (SEO T6.6 partial). Build 29 trang sạch + `qa.mjs` PASS + **verify headless chromium** (force-reduced-motion) 3 trang chủ/chi-nhánh/chương-trình = ẢNH HIỆN đúng + preview live serve 200 (homepage + 3 ảnh slot + branch page). ⚠️ `logo-512` nguồn hoá ra = icon Messenger (hỏng) → GIỮ F-badge; Drive creds + ảnh khách thật vẫn để Bill.

14. **DASH-FIX — Sửa luồng data token/cost Dashboard** (Bill duyệt 2026-06-17). ĐỌC `/root/lucy-workspace/dashboard-data-audit.md` TRƯỚC (audit + 7 lỗi + 5 quyết định + sprint S1-S5 chi tiết). GỐC: 2 sổ rời (agent-machine ledger worker-only vs token-guard counter mọi-đường) → ngày>tháng, 60M token mà $0, telegram không hiện. QUYẾT ĐỊNH BILL: TZ=VN(UTC+7) · pricing=tool fetch OpenRouter API + fallback hardcode · cache=cột riêng (in/out/cache kiểu Hermes) · forward-only (lịch sử cũ không khôi phục) · chạy auto-build.
    - ✅ **Round 1 [S1+S2+S3 backend] (DONE 2026-06-17, autobuild):** `types.ts` LedgerEntry +`source/model/cacheTok` (readLedger vá dòng cũ→source='worker',cacheTok=0); `engine.recordSpend()` = 1 ĐƯỜNG GHI (ledger), worker submit qua đó + **BỎ addTokens** → `token-guard.used` DẪN XUẤT `engine.ledgerUsedToday()` (Σ in+cache+out hôm nay, wire ở coordinator-main, cache 1 ngày + invalidate khi recordSpend) = hết double-count. Coordinator `POST /spend` đầy đủ + **tính usd 1 CHỖ qua `pricing.ts`** (`/token-guard/add` thành alias source='unknown'). `pricing.ts`: FALLBACK hardcode (opus 5/25·sonnet 3/15·haiku 1/5·fable 10/50, cacheRead 0.1×/cacheWrite 1.25×in — xác nhận qua claude-api skill) + `refreshOpenRouterPrices()` fetch lane (prefetch boot, fallback hardcode khi lỗi). Bơm `source+model+cacheRead/Write` vào: hub index.ts (×4: claude-path source='hub'+model thật tách cache, lane×2 source='lane', prompt-architect source='lane'), auto-build.py (autobuild/opus), bridge lucy_bridge.py (bridge, model=LAST_MODEL) + lane, cron report_tok.py (cron:<slug>, 4 .sh truyền slug+opus), autopilot-main director (lane/opus). **Cutover forward-only:** migrate 1 entry catch-up TỔNG token-day.json hôm nay (70.77M, source='unknown' usd=0 — KHÔNG bịa attribution) vào ledger để used() liên tục qua restart + giữ safety guard. tsc agent-machine+hub PASS · smoke-token-guard 43/43 · token-guard-bugs 13/13 · metrics 16/16 · bridge parity 22/22 · py-compile + bash -n OK. Restart lucy-coordinator (giữ full env /proc: AM_TOKEN 48 + AM_DATA + LUCY_VAULT) + lucy-hub (KHÔNG đụng lucy-bridge). **INVARIANT LIVE: token-guard.used == Σ ledger hôm nay = 70,769,768 ✓** · POST /spend opus usd=$0.019125 đúng công thức ✓.
    - ✅ **Round 2 [S4 dashboard] (DONE 2026-06-17, autobuild):** `metrics.ts`: `MetricsDay`/`MetricsGroup` +`cacheTok`, `Metrics` +`costBySource`, helper `add()` gom 1 entry→group (in/out/cache), `buildMetrics` ưu tiên `e.model` ghi thẳng (dòng cũ→resolve persona), `buildSeries` token gồm cache. Coordinator `/metrics`: tokenDay/Month +cache (= guard.used), `costByModel/Agent` +in/out/cache, mảng `costBySource` sort theo token. `api.ts`: `MetricsCostEntry/AgentEntry` +in/out/cache, type `MetricsSourceEntry`, `MetricsData.costBySource?`. `Dashboard.tsx`: **REVERT vá interim** Token/ngày→`data.tokenDay` (giờ = Σ ledger mọi nguồn+cache = guard.used, hết ngày>tháng); helper `TokBreakdown` (in·out·cache) + `SOURCE_LABEL`; cột in/out/cache cho Cost by Model + Cost by Agent; section **"Nguồn đốt"** (worker/autobuild/telegram/hub/cron/lane/unknown, thanh theo token). tsc AM+hub PASS · smoke-metrics 19/19 (thêm costBySource has worker + Σtoken src==model). Restart lucy-coordinator (giữ env /proc AM_TOKEN 48+AM_DATA+LUCY_VAULT) + lucy-hub (KHÔNG đụng bridge). **INVARIANT LIVE: tokenDay==guard.used=98,342,570 ✓ · tokenDay≤tokenMonth (98M≤136M) ✓ [P2 ngày>tháng HẾT] · costBySource=worker$146.81+unknown$0 (in/out/cache tách).** ⚠️ `costDay=0` hôm nay = NGOẠI LỆ forward-only đã ghi (hôm nay ledger chỉ source='unknown' usd=0 từ đường legacy /token-guard/add của bridge/cron CHƯA restart; worker priced sẽ vào khi có run). cacheTok=0 toàn bộ (entry mới qua /spend cacheReadTok sẽ lấp).
    - **Round 3 [S5 QA]:** TZ VN cho resetDay↔metrics; smoke reconcile (Σledger==used, ngày≤tháng, token>0⇒cost>0, double-count guard); perf ledger (rotate nếu chậm).
    - **INVARIANT nghiệm thu:** Token/ngày ≤ Token/tháng · Σ costBySource == costDay · token-guard.used == Σ ledger hôm nay · telegram có dòng · token>0 ⇒ cost>0.
    - ⚠️ Restart **lucy-coordinator** GIỮ ENV (AM_TOKEN 48-ký qua /proc) + **lucy-hub** (plain). **KHÔNG restart lucy-bridge** (bridge code-only, Bill tự restart). tsc AM+hub PASS + smoke trước khi restart.

> Mỗi bước phải dệt vào "MỘT Lucy", không đẻ silo mới. Build xong từng chi thể → verify → tiếp.
