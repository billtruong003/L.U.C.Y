# BUILD STATE — Prompt Architect (4 cụm Opus)

> Trạng thái thực thi để resume. Cập nhật 2026-06-15. Kiến trúc: `2026-06-15-prompt-architect-architecture.md`.

## Quy tắc thực thi (Bill chốt 2026-06-15)
- Chia 4 lần chạy Opus, mỗi cụm = 1 lần build trọn (code + build + verify).
- TRƯỚC mỗi cụm: check token (`ccusage blocks --active` + probe). Nếu gần cạn/limit → PACK UP, đợi hiệu lệnh.
- Build phải ADDITIVE + FEATURE-FLAG + rollback (theo chuẩn Lucy), KHÔNG restart service live khi chưa duyệt.

## Tiến độ cụm
- [x] 🅰️ CỤM A — Bộ não lõi (backend, không UI): task 1+2+5+6 XONG 2026-06-15. flag `LUCY_PROMPT_ARCHITECT` (mặc định TẮT). File mới: src/prompt-architect.ts (core: run+escalate+clarify-gate+extract), src/prompt-architect-store.ts (sidecar `<vault>/.index/prompt-architect.db`), src/smoke-prompt-architect.ts. Sửa tối thiểu: llm-lane.ts (+model `ds-chat`=deepseek/deepseek-chat via OpenRouter, prompt-cache tự động), package.json (script + persona JSON config/personas/prompt-architect.json). tsc PASS. smoke 29/29 PASS (gồm 1 LIVE: ds-chat gọi thật → clarify-gate hỏi 3 câu đúng). KHÔNG restart service, KHÔNG commit. Lưu ý: stack đã rời OmniRoute → dùng in-house llm-lane (callLLM thuần, KHÔNG tool = no-execute). CỤM B cần: wire flag vào hub endpoint/SSE + đọc store.recent() cho tab history + nút escalate gọi escalatePromptArchitect().
- [x] 🅱️ CỤM B — Mặt tiền Hub: XONG + LIVE 2026-06-15. File: hub/web/src/components/Prompts.tsx (tab), hub/web/src/App.tsx (wire tab sau flag /api/prompts/status), hub/web/src/api.ts, hub/server/src/index.ts (endpoint run/escalate/edit/history/status). Đã: thêm `LUCY_PROMPT_ARCHITECT=1` vào hub/server/.env; build web (npm run build → Prompts tab vào bundle); rehost CÓ MỤC TIÊU `pm2 restart lucy-hub` (plain, dotenv đọc lại flag). ⚠️KHÔNG dùng deploy.sh: nó `startOrReload ecosystem` reload luôn lucy-coordinator → mất token thật 48-ký-tự + AM_DATA live (xác nhận divergence qua /proc: live AM_TOKEN dài 48, có AM_DATA; ecosystem fallback chỉ 'lucytok'). VERIFY LIVE: status enabled:true; SSE run ngữ cảnh mơ hồ → ds-chat clarify-gate hỏi 3 câu, lưu session #1, history OK. Bridge + coordinator nguyên vẹn (pid coordinator không đổi). Lưu ý PA-D: cũng tránh deploy.sh, dùng build web + `pm2 restart lucy-hub`.
- [x] 🅲 CỤM C — Thông minh hoá: XONG + LIVE 2026-06-15. File mới: src/prompt-architect-intel.ts (scorePromptDraft rubric DETERMINISTIC 7 tiêu chí 0..100 + weak[] · derivePreferences/buildPreferencePreamble few-shot từ store.recent(chatId) chỉ áp khi ≥2 mẫu · targetModelStyleHint claude/gpt/gemini/nhỏ · extractAllPrompts/variantsDirective). Sửa: prompt-architect.ts (buildSystemAddendum chèn phong cách+style SAU scaffold; runPromptArchitect +usePreferences(mặc định BẬT khi có chatId)+variants(≥2); result +scorecard/variants/preferenceApplied; escalate +scorecard), hub/server/index.ts (/api/prompts/run nhận variants, SSE final phát scorecard/variants/preferenceApplied; escalate trả scorecard), smoke-prompt-architect.ts (+16 case → 44/44). tsc PASS cả AM+hub. Rehost CHỈ `pm2 restart lucy-hub` (KHÔNG deploy.sh — giữ coordinator env). VERIFY LIVE: login→status enabled; run variants=2 target=claude → 2 biến thể prompt thật, scorecard total=99 "xuất sắc" weak=[], preferenceApplied=true (đọc lịch sử hub-prompts). Bridge(28h)+coordinator(8h) pid nguyên. → CỤM D có thể render scorecard/variants từ SSE final + side-by-side dùng store.recent.
- [x] 🅳 CỤM D — UX nâng cao Hub: XONG + LIVE 2026-06-15. File mới: hub/web/src/components/promptDiff.ts (diffLines LCS + diffStat, không thêm dependency) + PromptCompare.tsx (CompareView side-by-side tô màu add/del · Scorecard chip total→thanh tiến trình từng tiêu chí+chỗ yếu · VariantTabs tab A..N + ⇄ so sánh 2 biến thể). Sửa: Prompts.tsx (Turn +scorecard/variants/preferenceApplied/prevPrompt; SSE final đọc meta; composer toggle "2 biến thể"→variants=2; chip 🎯 phong cách; escalate giữ prompt architect cũ → CompareToggle so sánh cũ↔Claude; panel Lịch sử nút ⇄ diff "Bản gốc↔Bản sửa"), api.ts (PromptArchEv +scorecard/variants/preferenceApplied; promptArchRun +param variants). tsc PASS + npm run build OK. Rehost CHỈ `pm2 restart lucy-hub` plain (KHÔNG deploy.sh). VERIFY LIVE (login cookie): status enabled; run variants=2 target=claude → final SSE scorecard.total=85, variants=5, preferenceApplied=true, finalPrompt 1460 ký tự; bridge(28h)+coordinator(8h) pid nguyên. → toàn bộ PA (A→D) DONE; còn lại item 10 PT (parity Telegram token+memory).

## Token log (cập nhật mỗi cụm)
- 2026-06-15 ~21:35: block đã đốt $21.46 (deep-research x2), còn ~1h block, burn ~$5.42/h. Đã dính session-limit 1 lần lúc 18:13 → vùng xài nặng.

## PT — Parity Telegram token+memory (item 10 MASTER-SPEC)
- [x] DONE 2026-06-15 (code-only, active khi Bill restart bridge). Audit: memory ĐÃ parity (recall+episodic+vault add-dir). GAP = token. FIX trong `bridge/lucy_bridge.py`: `_report_tok`/`report_tokens`/`_sdk_usage` → POST `/token-guard/add` (NGUỒN CHUNG) + counter cục bộ + lệnh `/token`. Wire 4 hàm claude + lane `run_lane`. inTok parity hub (gồm cache). Flag `LUCY_TOKEN_REPORT` (BẬT). smoke `bridge/smoke_token_parity.py` 18/18. KHÔNG restart bridge.

## Resume
Đọc file này + architecture.md → biết cụm nào tick, chạy cụm kế. Code đặt trong agent-machine (persona) + hub (tab). Flag tắt mặc định để an toàn live.
PA (A→D) + PT đều DONE → đợt ưu tiên này ALL_DONE.
