# secrets — never commit
.env
.env.*
!.env.example

# runtime
node_modules/
dist/
*.log
logs/

# upstream Hermes Agent source (cloned for reference, not Bill's code — cài thật trên VPS qua installer)
hermes-agent/

# research output (data, not code) — decide later if you want it tracked
research/*.md
!research/.gitkeep

# voice model weights / audio samples (large)
voice/models/
voice/samples/
*.wav
*.mp3

# os / editor
.DS_Store
Thumbs.db
